TODO:
    Color GAN:
        We build a tiny, lightweight and super simple GAN to generate color pallette given a short text.
        The GAN must be simple and lightweight.
        The loss of the generator in the current implementation drops instantly to 0, but the actual results look bad visually.

        Tasks:
            Suggest, one simple step to improve the GAN results.
            Avoid big architectures, complex loss functions, or any other complex changes.

        Note:
            Avoid L1 (or any other) reconstruction loss, the colors in the train set have large variance and many different colors can fit the same text.

        DEBUG:
            I want to debug the GAN.
            The settings right now is that the generator maps z -> colors and the discriminator maps colors -> real/fake.
            Training ends up with 0 loss for the generator, and high loss for the discriminator (much higher than random guess).
            This implies a bug in the discriminator or the train loop.
            Note that when examining the generated colors by hand they look bad (easy to distinguish from the real distribution), so the generator is not doing a good job.

            Please check the code and try to find the root cause.
            If you can't find the root cause, suggest a simple way to debug the GAN and find the root cause.

        NEXT STEPS:
            The model now has 2 tasks:
                1. Predict the feeling
                2. Generate a color palette

            Notes:
                - We use a common text encoder for both tasks.
                - The feeling head is the one who send gradient to the text encoder.
                - gen and tst just use the text encoding but do not send gradient to the text encoder.

            Tasks:
                - Connect the feeling head and add it to the train loop.
                - Log the feeling loss, the feeling acc and feeling acc @ 5.
                - Make the gen and tst conditional on the encoded text. gen is also conditional on z (random noise)

        NEXT STEPS:
            - Save the model with the best feeling acc @ 5.
            - Add early stopping to the training loop (acc @ 5).

        NEXT STEPS:
            Also wire the EmojiHead:
                - Emoji head outputs an emoji embedding and uses in batch InfoNCE loss to train the text encoder (see older commits).
                - log emoji acc @ 10
                - log also f1 score on feeling acc @5 and emoji acc @10 to the early stopping criteria.

            Notes:
                - All was already implemented in older commits.
                - Recover it to the new code.
                - Use the new code style and structure and make minimal edits to the new code.

        NEXT STEPS:
            - Extract the training loop from the model.py file to train.py
            - Extract all configuration constants to config.py
            - Recover the tensorboard naming convention from older commits where the name was derived from the timestamp + config in config.py.

            Notes:
                - Do not export the web app or anything related to it.
                - Do not export onnx model at this point.

        NEXT STEPS:
            - Update the GAN, condition on the emoji embedding instead of the text encoding, hold on if you think this is a bad call.

        DISCUSSIONS:
            1.  Should we first train the model (feeling + emoji) then train the GAN on a frozen model? What would it take code wise?
            2.  Before conditioned the ColorGen on the text encoding it generated beautiful and diverse color pallettes (but unrelated to text).
                After conditioning on the text encoding it generates monotone color pallettes that are not diverse at all.
                You can see examples at:
                    file:///home/gilad/Work/emojic/report/preview/pred-09-01-15:31.html (before)
                    file:///home/gilad/Work/emojic/report/preview/pred-09-01-15:55.html (after)

                What can we do about it? How can we increase the capacity of the generator.

        NEXT STEPS:
            Reenable the web page:
            - Create a script onnx.py that loads all the exported .pt models and exports them to onnx.
            - Adjust the web app to use the new architecture.
            - Emojis and Feeling bars stay the same (or almost the same)
            - Colors bar need to sample from the GAN 5 times.

        DATA QUALITY:
            - It seems that there isn't enough emoji coverage, like basic items, objects, nouns, etc..
            - We should check again the bun/data/train.ts prompt to understand if the core problem is there.
            - If it seems fine, the plan is to get more data, then export top 200 emojis to labels.json then upsample rare emojis.
            - we already have a script emoji2feelings.ts that should upsample but it might be outdated, please check it and change its name to upsample-emojis.ts.
            - e.g. for the text "I want to watch TV" I get the following emojis "🚶0.42🥰0.42👀0.40🙂0.37👋0.37❤️0.36🤔0.35🌟0.35🤝0.34💪0.33"
            - we also need to upsample feelings, check if the existing script feeling2emojis.ts is still relevant and change its name to upsample-feelings.ts.
            - finally, at some point we need a method to clean bad data, no need to implement yet, but maybe we should already add some metadata to the dataset so it can help us clean it later.

        TOOLS:
            Create a tool tools/data/preview-labels.ts
            The tool should read the labels.json file and generate a static html preview page with:
            - a grid of all the feelings and their corresponding style.
            - a grid of all the emojis.
            Design the page and the layout so the two grids always fill up the entire page (exactly)
            You should probably use grid layout for both emojis and feelings.
            Feeling cells should contain the name of the feeling and should have a wide aspect ratio.
            Emojis should be square.
            Use some padding but let the feeling and emojis take most of the space.
            The goal of this page is to let me review the content of the labels.json file in a single glance.

        DISCUSSION:
            When generating new data, do you think we can condition the text on a feeling and an emoji? Will 'openai/gpt-5.6-luna' handle this?
            If so, should we take this approach and replace both the emoji upsampling and the feeling upsampling scripts?

        Merge Existing feelings:
            The goal is to reduce the number of feelings to 30 and merge existing feelings with high similarity.
            Merging plan:
            1. propose how to merge the feelings.
            2. write a report report/merge/<timestamp>.md about the feeling distribution after the merge.
            3. If we decide to merge do not use alias map, instead update the feeling label, but keep the original feeling in the meta field.

        Feelings:
            Look at all the feelings that we have in the train.jsonl and make a list of feelings you think I should use.
            Do not consider existing frequency or anything else, just make a list (any length) that you think is a complete list:
            - No two feelings that are too similar 
            - No missing feelings.

            Write it to report/feelings/<timestamp>.md.

        Color GAN:
            We must replace the emoji embedding passed to the ColorGen with the text embedding instead.
            Make sure the train and inference code are all updated to reflect this change.
            Also make sure the onnx export code is updated to reflect this change.

        Ekman's six 
            We should go with Ekman's six basic emotions (anger, disgust, fear, happiness, sadness, surprise) + Neutral.
            Write a detailed report how this migration affect the product (model, web app, data, etc...)
            Especially, consider what should we do with all the existing data that we have.
            The report should be written to report/ekman/<timestamp>.md.
            The report must contains a practical, high level plan to migrate the existing project to Ekman's six basic emotions + Neutral.

        Implement The Migration:
            Implement according to the report you wrote at report/ekman/09-02-01:10.md
            Implement Phase 1 and 2.
            Then report and wait for me to run the upsampling scripts before rebuilding the eval.jsonl set (Phase 4)
            

            Comments:
                Important:
                - Feeling metric is now acc @ 1, that is correct.
                - Do not weight the classes in the nn.CrossEntropyLoss() loss, we will balance the classes by either upsampling or downsampling the data.
                - Design premise: make sure every feeling has a truly unique and distinct style.
                - Use Deterministic fold to map the existing data into the new feelings, remove the metadata field.
                - Drop feelings that are not clearly mapped to the new feelings.
                - No META field in the new data at all, remove it from the pipeline completely.
                - During the migration deduplicate text based on its normalized form, pick one of the annotations and drop the rest.
                - Rebuild eval.json as per the existing skill. eval.jsonl must be flawless and top quality set.
                - Use the canonical strings (Angry, Disgusted, Afraid, Happy, Sad, Surprised, Neutral).

                Minor:
                - Your information about the Colour GAN is a bit out-dated but does not affect the migration.

        Upsampling:
            Update the upsampling feelings script to include topics that will steer the generated text towards certain classes of emojis.
            The goal is to avoid too many emoji faces and to add miss-covered classes like objects, animals, plants, etc...
            When annotating using topics add a {topic} field to the data (it is fine that existing records do not have this field.)

        Fail Analysis:
            Create a tool tools/analysis/fails.ts that run the trained model on the eval.jsonl set and generate a report of the fails.
            List all the failed records ordered by the model error.
DONE:

