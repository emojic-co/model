Update the upsample tool add a standalone option:
    upsample --flags
    This option should pick all the `flag: <country>` emojis:
    Ask the text generator to generate a short text related to the country and the emoji flag.
    Ask the annotator to annotate the text, steer the color pallet towards the colors of the flag.
    Final records is {text: <generated text>, emoji: <flag emoji + annotator emojis>, fg, bg}
    Where fg and bg should (but not enforced) be the colors of the flag.

    respect the --per (default 50) flag