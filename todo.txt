Done:
update regen: normalize all keywords before writing `keywords.jsonl` and drop any normalized keyword that is less than 3 chars. same for the `terms.jsonl`
report `Model — Encoder block capacity`: use a large batch (every valid sample per source) instead of one input and report the average result; removed the `text` column, added an `N` column.
