So each run generates a subfolder under the `pt` folder with <timestamp>-<git short commit hash> as the name. The subfolder contains all .pt files.
in the `pt` folder just use a simple version.txt file the points to the actual model subfolder.

1. What happens if the logic around the model changes?