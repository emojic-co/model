TODO:
    Write a script that uses (Vercel) ai sdk and GPT-5.6 Luna to generate the dataset.
    The script should be ran from the command line and appends to data.jsonl.
    The script picks a random feeling and emoji, and generates 10 samples, note that the model should only return the text (feeling and emoji are given).
    Text should be short, 1-6 words and should have the style of a WhatsApp message.
    Use the AI_GATEWAY_API_KEY from the .env file
