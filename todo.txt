TODO:
    So lets go with option 1 (Structured jitter).
    Your tasks:
        - The web interface changes a bit, now the user can pick:
            - emoji
            - color 
            - feeling (for now is just the font)

    Notes:
        - The first color is the prediction of the model, then 4 more colors are generated by jittering the original color.
        - UI stays the same but replace the 5 feeling row with 2 rows: 5 colors and 5 feelings.
        - The feeling cards now have neutral colors (gray) but they use the style of the feeling and the name of the feeling.
        - On mobile both color bar and feeling bar appears as 2x2 grid (last color and feeling are dropped).

DONE:
