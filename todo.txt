Done:

create a tool tools/analysis/emoji-coverage.ts

The tool reads the `goals.yml`, `data/groups.json` and the data/labels.json files and generates a file `emoji-coverage.html`
That list all the emojis by groups and their current coverage.
