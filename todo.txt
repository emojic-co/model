TODO:

DONE:
    - Github Page: "click to copy" button on the output card. Hand-draws the
      card (135deg gradient + emoji + wrapped text + feeling label, in the
      feeling's webfont) onto a 512x512 canvas -- no html2canvas-style dep --
      and writes the PNG to the clipboard via navigator.clipboard.write /
      ClipboardItem. oklab palette is converted to sRGB in JS so the export
      matches even where <canvas> doesn't accept oklab(). site/ only.

    - Standalone web page (no backend):
        - export the model to ONNX (build_web.py -> docs/model.onnx)
        - load it in the webpage using onnxruntime-web (vendored in vendor/ort/)
        - run the inference in the browser (site/app.js)
        - output the page to docs/ for GitHub Pages
        - automated: .github/workflows/deploy-pages.yml rebuilds + deploys on
          push to main (or manual workflow_dispatch); single local command is
          `uv run build_web.py`

    - at the bottom of the page, show examples of all feelings:
        Make a row of small squares, each square has the feeling name with the feeling font family and the feeling bg gradient.
