Report:
    update the report:
        Data:
            Show normalized length distribution for the data/train.jsonl instead of data/data.jsonl
        Emoji:
            Run the tools/analysis/cldr-baseline.ts and whenever reporting performance for the model also report the baseline performance.
            e.g. in the accuracy line chart also add the best baseline accuracy line chart.