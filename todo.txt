TODO:
    Android APP:
    - actual card and preview style cards do not match.

Done:
style.yml:
    -   refactor the structure of the .yml file.
        create a `patterns` section where we map a pattern name to a literal svg.
        then for each style just reference the pattern name instead of the svg.
