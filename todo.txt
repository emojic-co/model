So both keywords.jsonl and terms.jsonl should not have the colors field. 
When training the model, these records should only be used to train the EmojiHead.
Make sure the StyleHead and ColorCritic do not see these records. (StyleHead can technically see these records with an empty style list if it makes the code simpler)
Does it make sense?