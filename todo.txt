TODO:
DONE:
    Web App:
        - when pressing enter trigger the copy flow, i.e. copy the image to the clipboard and show a toast message.
    When reporting emoji acc report top-3, and top-5 accuracy.
    Tensorboard:
        Eval:
        - Only log feeling loss and emoji loss (no accuracy)
    Training:
        - Do not select model based on accuracy, use the loss to select the best model.
    Emoji Embedding:
        Lets move to learnable emoji embeddings and use contrastive loss to train the model.
        Update the model to reflect this change.
        Start with the simplest possible model implementation.
