During GAN training do not use shuffled (text, color) pairs and do not use random  colors to train the critic.
During GAN training the critic only gets:
1/2 real (text, color) pairs
1/2 fake (text, color) pairs