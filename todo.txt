TODO:
    Color GAN:
        We build a tiny, lightweight and super simple GAN to generate color pallette given a short text.
        The GAN must be simple and lightweight.
        The loss of the generator in the current implementation drops instantly to 0, but the actual results look bad visually.

        Tasks:
            Suggest, one simple step to improve the GAN results.
            Avoid big architectures, complex loss functions, or any other complex changes.

        Note:
            Avoid L1 (or any other) reconstruction loss, the colors in the train set have large variance and many different colors can fit the same text.

        DEBUG:
            I want to debug the GAN.
            The settings right now is that the generator maps z -> colors and the discriminator maps colors -> real/fake.
            Training ends up with 0 loss for the generator, and high loss for the discriminator (much higher than random guess).
            This implies a bug in the discriminator or the train loop.
            Note that when examining the generated colors by hand they look bad (easy to distinguish from the real distribution), so the generator is not doing a good job.

            Please check the code and try to find the root cause.
            If you can't find the root cause, suggest a simple way to debug the GAN and find the root cause.

        NEXT STEPS:
            The model now has 2 tasks:
                1. Predict the feeling
                2. Generate a color palette

            Notes:
                - We use a common text encoder for both tasks.
                - The feeling head is the one who send gradient to the text encoder.
                - gen and tst just use the text encoding but do not send gradient to the text encoder.

            Tasks:
                - Connect the feeling head and add it to the train loop.
                - Log the feeling loss, the feeling acc and feeling acc @ 5.
                - Make the gen and tst conditional on the encoded text. gen is also conditional on z (random noise)
DONE:
