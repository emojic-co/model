I want to run a full re-annotation task, emoji only:
    The annotator see the text and all the already existing emojis and it should expand the emoji list (do not force it to)
    Update the `upsample` tool, add this flag and respect the `--count` flag.
    Result records should include a meta fields indicating that this is a re-annotation task, and the additional emojis.
    Make sure to avoid reannotating records that already have the `reannotated` meta field.
    Make a concise specific prompt for this task, do not use the standard annotation pipeline.
    Use high concurrency for this task.