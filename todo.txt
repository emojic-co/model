TODO:
    Generating new data:
        update the gen_data.ts script:
            - generate raw texts - WhatsApp style, short, diverse topics, 1 message per line, no schema.
            - save the texts in a raw .txt file.
            - send the texts for annotations, similar to fix.ts:
                - batches of 10, assign id for each text and ask the model to annotate with emoji (any) and a feeling (from a closed list in labels.json).
                - keep records if either emoji already exists in the labels.json or if it has >= 1% frequency (in the current generation run).
DONE:
