Tools:
    Write a tool tools/data/stat.ts which prints statistics about the data files.
    Print important statistics that will help me understand the data better.
    In particular print the distribution of number of unique emojis per normalized text:
    text | # of records with this text | # of unique emojis for this text | list of unique emojis for this text
    Print to the terminal but use tables, colors, etc...
    You can use https://github.com/vadimdemedes/ink or a similar library to make the output more readable.
    Keep the output concise with only the most important statistics.
    Add a flag --long to print more statistics.


Model:
    It seems that the TextEncoder + EmojiHead model now perform well on keywords but could perform better on the whole text context.
    I want to increase its receptive field, suggest how.

Done:
    Report:
        Update the report tool:
            Before the "missed keywords" section add a section "missed texts".
            In this section, list the worst 50 texts from the eval.jsonl
            worst means the rank of the first relevant emoji is the highest (i.e. the worst).
            show a table with the text, target emojis, top 5 predicted emojis, and the rank of the first relevant emoji.
