
Tools: 
    Upsample:
        Create a set of 10 generic color pallets, whenever the annotator failed to provide a color palette use a random one from the set instead of dropping the record.

Tools:
    update the tools/data/cldr.ts:
        add a flag --missing
        with this flag the script append to data/cldr.json the missing keywords instead.