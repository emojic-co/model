Done:
Similar to the `train energy` command,
I would like to have a `train critic` command that only train the critic network.
This is for debugging purposes, and no other network should be used.
Th train only feeds the critic with real (text,colors) pairs and with fake.
The fake once are created by just shuffling the real ones.
Please log the r^2 score of the critic.