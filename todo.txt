Simplify the train script:
there only 2 options to run this script:
1. train - train the text encoder, then the GAN model.
2. train gan - train only the GAN model, using a pretrained text encoder.

both options accept the --local flag.


all other subcommands and flags should be removed. 