Also, slicing the data.jsonl into train and eval should always be done via the regen command.
You should set its params according to the goals and to the coverage needed, you can use its --matrix flag to help you decide on the correct split.