TODO:
1.  We need to make sure that the android and web apps produce identical visual results.
    Create a .yml file on the web that the android app syncs along with the model weights.
    The file should describe:
    - global settings:
        - max opacity
        - font size calculation
        - etc...

    - For each style:
        - Font family
        - Font weight
        - bg pattern (literal SVG not just the name)
        - etc...

    Make sure that both android and web apps use this file to render the card.

3. On android, it is enough to have just the "share" button, make it obvious and follow common design patters for a shared button.
4. On web:
    "copy" just generates and copies the image to the clipboard.
    "share" shares the link to the card.

Done:
Make sure that the generated image from the card does not have any "share" "copy" buttons not related to the card itself.