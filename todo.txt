Update the generator to have text embedding + noise instead of [text, noise] concatenated.
Also when running `train energy` use  actual text embeddings, pre encode all texts and cache them into a .pt file (same as `train critic`)