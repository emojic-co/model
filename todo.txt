Done:

add `upsample sarcasm` options.
It should generate texts with sarcasm.
Note:
    upsample, unless explicitly given the `--lang` flag, should always upsample in every language (English, Hebrew for now).