data.py
    Whenever a record has more than `MAX_EMOJIS_PER_SAMPLE` emojis the ds should pick a random subset of `MAX_EMOJIS_PER_SAMPLE` emojis to use for training.
    Make sure every time this record is sampled it picks a different random subset of emojis.

Keyword Sampling:
    1. regenerate the data/keywords.jsonl file, add a field "word_count" to each record.
    2. consider each record as 'single' / 'double' / 'multi'
    3. at train time sample from the keywords.jsonl according to this prob in the `config.py` file.:
    ```
    @dataclass
    class KEYWORDS_SAMPLING_RATE:
        single = 0.1
        double = 0.01
        multi = 0.01
    ```
    4. In TB logs only report the accuracy for a single keyword.
    5. In the report report the accuracy breakdown for single / double / multi keywords.
    6. Note that `goals.yml` was updated, review it and adjust the numbers.
