# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.vscode
# model.pt is committed: CI reads it to build the Pages site.
*.bak
.env

# Built Pages site -- CI regenerates it from model.pt and uploads it as the
# Pages artifact. Build locally for preview with `uv run build_web.py`.
/docs/

# TensorBoard logs
runs/

# Node / Bun (gen_data.ts)
node_modules/

data.jsonl.dry